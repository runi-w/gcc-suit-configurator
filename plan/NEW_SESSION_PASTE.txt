I'm continuing work on the Gage Court suit configurator. Everything is in
~/Desktop/GCC_House_Model/ (a git repo, committed locally — nothing pushed yet).

Read these first, in this order:
- ~/Desktop/GCC_House_Model/HANDOVER.md — full state, file layout, locked constants, next actions
- the `gcc-pattern-render-playbook` memory — THE STANDARD for fabric rendering. It is locked and
  carries a tried-and-rejected list. Read before touching any rendering code.

Rebuild (byte-identical, all paths absolute):
  cd ~/Desktop/GCC_House_Model/builder && python3 build_configurator_v0.py
Preview over http:// (not file://). Live preview artifact:
  https://claude.ai/code/artifact/d1bb05f8-0a3e-42b6-b87c-5338010b729b

CREDENTIALS: never in the repo. Gemini key + KuteTailor creds live in
~/Desktop/GCC_Fabric_Handoff/keys/ and are read at runtime. Don't inline any secret.
The git history is clean from commit one — keep it that way.

SETTLED — do not relitigate:
- 2D compositing, NOT real-time 3D. Proven from live production assets that Suitsupply and
  Hockerty are both pre-rendered 2D layer stacks. Don't propose 3D.
- Do NOT add AO / crevice darkening in folds. Tried, rejected, and Suitsupply measurably have none.
- Our asset count is fabric-independent (we composite cloth at runtime). ~126 renders covers every
  visible option across all 117 cloths. Do NOT copy Suitsupply's 25,000-layer per-fabric approach.
- AI cannot do the garment CAD (researched July 2026). Don't re-propose it as a shortcut.
- Fabric names come from Shopify, never guessed from colour.
- Suits are made-to-measure — never "handcrafted" or "hand-tailored".

MEASUREMENT RULES (each of these already cost a wasted round):
- Hard-mask alpha>200, and erode 3px on a variance mask. The anti-aliased edge band inflated
  measured specular from 3.3 to 10.9 and looked like a real defect.
- Compare images at MATCHED px/cm. Mismatched density made our pattern look 2x wobblier than it is.
- Use Suitsupply's `ai-generated/ai-model` layer as reference, never their flat `Jacket/model`
  layers — a garment with no body inside has neckline-cavity darks ours can't have.
- audit/period.py is unreliable (locks onto harmonics). Settle scale questions visually.
- Metrics miss things. An edge-roughness metric rated a broken mask identical to a good one; only
  a 6x visual zoom found it. Always look at the image, don't just trust the number.

CURRENT STATE (measured vs Suitsupply's photographic model layer):
  MATCHED: colour 98% of the mill scan, pattern scale 0.99x true, sharpness 95% of true cloth,
           no moire, specular 3.08 vs their 3.30, on-screen density 9.95 vs their 10.3 px/cm.
  GAPS:    crease depth 0.24 vs their 0.032 (~7x lighter)
           pattern wrap: ours 5.4 deg of noise vs their 22.7 deg monotonic swing
  NOTE: only the `notch` cut has the new house model; the other 4 cuts still use old renders, so
        the man changes when you switch cut. Expected — fixed by generating the remaining 14.

TOP NEXT ACTIONS (full list in HANDOVER.md section 6):
  1. Pattern wrap (SIL_WRAP) — implemented in buildWarpNormal, validated on the torso, DISABLED
     because hw[y] spans the whole row so the sleeves get sheared as if they were the torso.
     Needs per-panel centre/width (torso vs each sleeve). Target: swing 22.7deg, spread 16.7deg,
     monotonic. Measure with: python3 audit/pattern_map.py
  2. Crease depth — lever is the base render's own creases (a hero prompt change), not the
     compositor. Decide before generating the other 14.
  3. Generate the remaining 14 renders — plan/MODEL_BIBLE.md has the spec, order, and all prompts.
  4. Push to GitHub — private repo under runi-w, matching fabric-stock-checker; PAT in Keychain.

WHAT I WANT TO WORK ON: [FILL IN]
